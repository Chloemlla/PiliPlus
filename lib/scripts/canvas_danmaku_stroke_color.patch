diff --git a/lib/models/danmaku_content_item.dart b/lib/models/danmaku_content_item.dart
--- a/lib/models/danmaku_content_item.dart
+++ b/lib/models/danmaku_content_item.dart
@@ -12,6 +12,9 @@ class DanmakuContentItem<T> {
   /// 弹幕颜色
   Color color;
 
+  /// Optional per-item stroke color. Null keeps the package default behavior.
+  final Color? strokeColor;
+
   /// 弹幕类型
   final DanmakuItemType type;
 
@@ -29,6 +32,7 @@ class DanmakuContentItem<T> {
   DanmakuContentItem(
     this.text, {
     this.color = Colors.white,
+    this.strokeColor,
     this.type = DanmakuItemType.scroll,
     this.selfSend = false,
     this.isColorful = false,
@@ -73,6 +77,7 @@ class SpecialDanmakuContentItem<T> extends DanmakuContentItem<T> {
     super.text, {
     required this.duration,
     required super.color,
+    super.strokeColor,
     required this.fontSize,
     this.hasStroke = false,
     required this.translateXTween,
@@ -96,6 +101,7 @@ class SpecialDanmakuContentItem<T> extends DanmakuContentItem<T> {
     double videoY = 1080,
     bool disableGradient = false,
     T? extra,
+    Color? strokeColor,
   }) {
     final (startX, endX) = _toRelativePosition(list[0], list[7], videoX);
     final (startY, endY) = _toRelativePosition(list[1], list[8], videoY);
@@ -139,6 +145,7 @@ class SpecialDanmakuContentItem<T> extends DanmakuContentItem<T> {
       text,
       duration: duration,
       color: color,
+      strokeColor: strokeColor,
       fontSize: fontSize,
       hasStroke: hasStroke,
       alphaTween: alphaTween,
diff --git a/lib/utils/utils.dart b/lib/utils/utils.dart
--- a/lib/utils/utils.dart
+++ b/lib/utils/utils.dart
@@ -74,18 +74,19 @@ abstract final class DmUtils {
         fontWeight: FontWeight.values[fontWeight],
         textDirection: TextDirection.ltr,
         maxLines: 1,
       ));
+      final customStrokeColor = content.strokeColor;
       final Paint strokePaint = Paint()
-        ..shader = content.isColorful
+        ..shader = customStrokeColor == null && content.isColorful
             ? const LinearGradient(
                     colors: [Color(0xFFF2509E), Color(0xFF308BCD)])
                 .createShader(Rect.fromLTWH(0, 0, w, h))
             : null
         ..style = PaintingStyle.stroke
         ..strokeWidth = strokeWidth;
 
-      if (!content.isColorful) {
-        strokePaint.color = Colors.black;
+      if (customStrokeColor != null || !content.isColorful) {
+        strokePaint.color = customStrokeColor ?? Colors.black;
       }
 
       if (content.count case final count?) {
@@ -138,8 +140,13 @@ abstract final class DmUtils {
       ..pushStyle(ui.TextStyle(
         color: content.color,
         fontSize: content.fontSize,
-        shadows: content.hasStroke
-            ? [Shadow(color: Colors.black, blurRadius: strokeWidth)]
+        shadows: content.hasStroke || content.strokeColor != null
+            ? [
+                Shadow(
+                  color: content.strokeColor ?? Colors.black,
+                  blurRadius: strokeWidth,
+                ),
+              ]
             : null,
       ))
       ..addText(content.text);
