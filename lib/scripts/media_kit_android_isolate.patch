diff --git a/media_kit/lib/src/player/native/core/initializer.dart b/media_kit/lib/src/player/native/core/initializer.dart
index 7638005..145a3ee 100644
--- a/media_kit/lib/src/player/native/core/initializer.dart
+++ b/media_kit/lib/src/player/native/core/initializer.dart
@@ -7,0 +8 @@ import 'dart:ffi';
+import 'dart:io';
@@ -35,0 +37,6 @@ abstract final class Initializer {
+    // Android is the exception: it always runs the isolate event loop, because
+    // that loop reports when it has stopped, which is what lets disposal destroy
+    // the mpv context only after nothing is left waiting on it.
+    if (Platform.isAndroid) {
+      return InitializerIsolate.create(callback, options: options);
+    }
@@ -50 +57,8 @@ abstract final class Initializer {
-  static void dispose(Pointer<generated.mpv_handle> ctx) {
+  ///
+  /// On Android the returned future completes only once the event loop driving
+  /// the handle has stopped, so the caller can destroy the context safely.
+  static Future<void> dispose(Pointer<generated.mpv_handle> ctx) async {
+    if (Platform.isAndroid) {
+      await InitializerIsolate.dispose(ctx);
+      return;
+    }
@@ -52 +66 @@ abstract final class Initializer {
-      InitializerIsolate.dispose(ctx);
+      await InitializerIsolate.dispose(ctx);
@@ -60 +74 @@ abstract final class Initializer {
-      InitializerIsolate.dispose(ctx);
+      await InitializerIsolate.dispose(ctx);
diff --git a/media_kit/lib/src/player/native/core/initializer_isolate.dart b/media_kit/lib/src/player/native/core/initializer_isolate.dart
index eeaa93a..e1995c4 100644
--- a/media_kit/lib/src/player/native/core/initializer_isolate.dart
+++ b/media_kit/lib/src/player/native/core/initializer_isolate.dart
@@ -31,0 +32 @@ abstract final class InitializerIsolate {
+    int? handleAddress;
@@ -39,0 +41 @@ abstract final class InitializerIsolate {
+        handleAddress = message;
@@ -51,0 +54,8 @@ abstract final class InitializerIsolate {
+        // The isolate has left its event loop and will not touch the handle
+        // again; releasing this future is what lets disposal proceed.
+        final shutdown = handleAddress == null
+            ? null
+            : _disposeCompleters.remove(handleAddress);
+        if (shutdown != null && !shutdown.isCompleted) {
+          shutdown.complete();
+        }
@@ -66 +76,4 @@ abstract final class InitializerIsolate {
-  static void dispose(Pointer<generated.mpv_handle> handle) {
+  ///
+  /// Completes when the isolate event loop has stopped, so callers can destroy
+  /// the mpv context without racing a loop that is still waiting for events.
+  static Future<void> dispose(Pointer<generated.mpv_handle> handle) {
@@ -69,2 +82,3 @@ abstract final class InitializerIsolate {
-    if (port != null && isolate != null) {
-      port.send(null);
+    if (port == null || isolate == null) {
+      return Future.value();
+    }
@@ -72,2 +86,2 @@ abstract final class InitializerIsolate {
-      _ports.remove(handle.address);
-      _isolates.remove(handle.address);
+    final shutdown = Completer<void>();
+    _disposeCompleters[handle.address] = shutdown;
@@ -75 +89 @@ abstract final class InitializerIsolate {
-      NativePlayer.mpv.mpv_wakeup(handle);
+    port.send(null);
@@ -77,4 +91,16 @@ abstract final class InitializerIsolate {
-      Future.delayed(const Duration(seconds: 2), () {
-        isolate.kill(priority: Isolate.immediate);
-      });
-    }
+    _ports.remove(handle.address);
+    _isolates.remove(handle.address);
+
+    // mpv_wait_event(-1) blocks the isolate, so the shutdown message only
+    // reaches its receiver once this wakeup ends that wait.
+    NativePlayer.mpv.mpv_wakeup(handle);
+
+    // An isolate that already died never acknowledges; kill it so the caller is
+    // not left waiting for a reply that cannot arrive.
+    Future.delayed(const Duration(seconds: 2), () {
+      if (shutdown.isCompleted) return;
+      isolate.kill(priority: Isolate.immediate);
+      shutdown.complete();
+    });
+
+    return shutdown.future;
@@ -141,0 +168 @@ abstract final class InitializerIsolate {
+  static final _disposeCompleters = HashMap<int, Completer<void>>();
diff --git a/media_kit/lib/src/player/native/player/real.dart b/media_kit/lib/src/player/native/player/real.dart
index a45fef3..7964132 100644
--- a/media_kit/lib/src/player/native/player/real.dart
+++ b/media_kit/lib/src/player/native/player/real.dart
@@ -116,8 +116,8 @@ class NativePlayer extends PlatformPlayer {
-    Initializer.dispose(ctx);
-
-    Timer(const Duration(seconds: 5), () {
-      mpv.mpv_terminate_destroy(ctx);
-      for (var i in _requests.values) {
-        if (!i.isCompleted) {
-          i.completeError(AssertionError('[Player] has been disposed'));
-        }
+    // Android waits here for the event loop to stop, so the context is destroyed
+    // only after nothing is left waiting on it.
+    await Initializer.dispose(ctx);
+
+    mpv.mpv_terminate_destroy(ctx);
+    for (var i in _requests.values) {
+      if (!i.isCompleted) {
+        i.completeError(AssertionError('[Player] has been disposed'));
@@ -125,2 +125,2 @@ class NativePlayer extends PlatformPlayer {
-      _requests.clear();
-    });
+    }
+    _requests.clear();
